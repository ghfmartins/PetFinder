export interface IUserRetorno {
    name: string,
    email: string,
    logradouro: string | null,
    numero: number | null,
    complemento: string | null,
    bairro: string | null,
    cidade: string | null,
    estado: string | null,
    telefone: string | null,
    CEP: string | null
}