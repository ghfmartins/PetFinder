export interface PetInterface {
  especie: string,
  raca: string | null,
  cor: string | null,
  nome: string | null,
  idade: string | null,
  sexo: string | null,
  castrado: boolean | null,
  vacinas: string | null,
  urlFoto: string | null
}